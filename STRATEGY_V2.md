# 单标的策略说明（V2.1）

> TANGLI FUND DISCIPLINE · PERSONAL STRATEGY SYSTEM  
> **Designed by 汤力 · Built with ChatGPT · 2023—2026**

## 1. 这套系统解决什么问题

系统只回答一个问题：

> 对当前打开的这一只基金或 ETF，按照预先设定的规则，现在应该等待、买入、继续持有，还是卖出？

每只标的独立计算。其他基金的资金、盈亏、仓位和信号不会影响它。

组合首页仍会显示全部持仓的总市值和总盈亏，但这只是汇总信息，不参与策略判断。

## 2. 每只标的需要设置的参数

| 参数 | 含义 |
|---|---|
| `capital` | 准备用于这只标的的资金上限 |
| `base_ratio` | 第一档买入金额占该标的资金上限的比例 |
| `entry_threshold` | 从前期高点回撤多少开始买入 |
| `tier_step` | 后续每增加多少回撤进入下一档 |
| `multiplier` | 每深入一档，买入金额放大多少倍 |
| `sell_rule` | 趋势止盈、均值回归、长期持有或利差定投 |

`capital` 是这只标的自己的策略资金，不是全部账户资金。例如给某基金设置 `capital = 10,000`，代表该策略最多向这只基金累计投入 10,000 元。

## 3. 回撤阶梯买入

### 回撤计算

```text
回撤率 = (前期高点 - 当前净值) / 前期高点 × 100%
```

### 档位计算

```text
当前档位 = floor((当前回撤 - 入场阈值) / 档位步长)
```

只有回撤达到入场阈值时才进入第1档。

### 买入金额

```text
第 N 档金额 = 标的资金上限 × 基础比例 × 乘数^(N-1)
```

假设：

- 该标的资金上限：10,000 元
- 入场回撤：5%
- 档位步长：3%
- 基础比例：3%
- 乘数：1.5

则：

| 回撤区间 | 操作 | 计划金额 |
|---|---|---:|
| 小于5% | 等待 | 0 |
| 5%–8% | 第1档 | 300元 |
| 8%–11% | 第2档 | 450元 |
| 11%–14% | 第3档 | 675元 |
| 14%–17% | 第4档 | 1,012.50元 |

每个完整档位只执行一次。页面刷新或重复检查不会重复生成同一档订单。

如果行情直接从第1档跳到第3档，系统会识别跨过的档位，但累计买入绝不会超过这只标的自己的剩余资金：

```text
标的剩余资金 = capital - total_invested
```

## 4. 卖出规则

### 趋势型 `momentum`

适合波动较大的成长指数：

```text
卖出参考价 = 前期高点 + (前期高点 - 本轮最低点) / 2
```

它要求价格不仅回到前高，还要继续向上确认趋势，因此持有时间可能较长。

### 均值回归型 `mean_revert`

适合相对低波动品种：

```text
卖出参考价 = 前期高点
```

当价格从回撤区恢复到前期高点附近时给出卖出参考。

### 长期持有型 `none`

只跟踪净值、持仓和盈亏，不自动生成买入或卖出信号。

卖出提示只是规则信号，不会自动连接券商交易。

## 5. 利差定投 `spread`

利差策略用于红利 ETF：

```text
利差 = 滚动365天股息率 - 对应日期十年期国债收益率
```

默认规则：

| 利差 | 操作 |
|---|---|
| 高于3% | 按月度基准金额的2倍买入 |
| 1%–3% | 按月度基准金额买入 |
| 低于1% | 给出清仓信号 |

执行限制：

- 每个自然月最多执行一次买入；
- ETF 按100份整手计算；
- 实际金额不能超过这只 ETF 的剩余策略资金；
- 历史回测必须使用对应日期的国债收益率，不能用今天的利率回填历史。

## 6. 如何看单只标的详情页

建议按这个顺序阅读：

1. 看当前净值和当前回撤。
2. 看页面给出的“交易信号”。没有信号就不操作。
3. 看该标的资金上限、累计投入和剩余资金。
4. 买入前确认当前是第几档以及本档计划金额。
5. 实际成交后使用“记录交易”登记，系统才会推进档位和更新持仓。
6. 使用历史回测检查该标的参数，而不是拿其他基金的结果替代。

## 7. 如何理解新版回测

回测包含：

- 初始现金和现金不足限制；
- 买入、卖出费用；
- 总收益、年化收益、最大回撤和年化波动；
- 同期买入并持有基准；
- 同期每月等额定投基准；
- 后30%数据的样本外结果；
- 邻近参数的敏感性区间。

如果策略只在一个非常精确的参数组合下表现好，而相邻参数明显变差，应视为不稳定。回测是检验规则的工具，不是未来收益保证。[CFA Institute：Backtesting and Simulation](https://www.cfainstitute.org/insights/professional-learning/refresher-readings/2026/backtesting-and-simulation)、[SSRN：Probability of Backtest Overfitting](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253)

## 8. 日常使用流程

```text
打开某只标的
  → 更新行情
  → 阅读本标的信号
  → 核对档位、金额和剩余资金
  → 自己决定是否成交
  → 成交后记录交易
```

不要因为组合中另一只基金上涨、亏损或资金用完而改变这只标的的程序化信号。如果希望控制总体风险，应在系统外决定每只标的各自的 `capital`，系统内部仍按单标的独立执行。

## 9. 重要边界

- `fund_state.json` 是策略记录，不是券商真实账户。
- 系统不会自动下单。
- 净值和利率数据可能延迟或获取失败。
- 手工成交后必须登记，否则档位、成本和剩余资金会失真。
- 同一指数持有多只基金会分别产生信号；这是独立策略模式的预期行为。
- 基金费用和份额类别可能不同，设置策略前应核对实际申购、赎回及管理费用。[FINRA：Mutual Funds](https://www.finra.org/investors/investing/investment-products/mutual-funds)

---

“汤力策略精选”与“汤力俭账”采用统一的个人产品设计体系：深海军蓝代表纪律与稳定，雾蓝代表数据与过程，暖金焦点代表经过规则确认的行动时刻。策略图标中的曲线、回撤谷底和三条档位刻度，对应“观察—等待—分档执行”的核心方式。
